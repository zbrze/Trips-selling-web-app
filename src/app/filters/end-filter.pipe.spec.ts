import { EndFilterPipe } from './end-filter.pipe';

describe('EndFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EndFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
