import { MinPriceFilterPipe } from './min-price-filter.pipe';

describe('MinPriceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MinPriceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
