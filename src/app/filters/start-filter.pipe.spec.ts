import { StartFilterPipe } from './start-filter.pipe';

describe('StartFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StartFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
